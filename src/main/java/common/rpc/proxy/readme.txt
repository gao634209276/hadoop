4.动态代理和反射简述
在Java中，动态代理主要用来做方法的增强，可以在不修改源码的情况下，增强一些方法。
另外，还有一个作用就是做远程调用，
比如现在有Java接口，该接口的实现部署在非本地服务器上，
在编写客户端代码时，由于没法直接生成该对象，这个时候就需要考虑使用动态代理了。

而反射，利用了Class类作为反射实例化对象的基本应用，对于一个实例化对象而言，
它需要调用类中的构造方法，属性和一般方法，这些操作都可以通过反射机制来完成。
下面我们用一个实例来理解这些理论。
5.1动态代理
    JProxy
5.2反射
    JReflect
